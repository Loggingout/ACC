// bannerTypes.js — site banner categories
const BANNER_TYPES = Object.freeze({
  MAINTENANCE: 'maintenance',
  ANNOUNCEMENT: 'announcement',
  SERVICE: 'service',
});

module.exports = { BANNER_TYPES };
